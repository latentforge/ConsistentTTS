# Dia

This model is implemented directly in the [`transformers-tts`](https://github.com/latentforge/transformers-tts)
fork (`src/transformers/models/dia/`). This folder only relays those classes; it does
not vendor or reimplement the model.

Original model and code: https://github.com/nari-labs/dia
